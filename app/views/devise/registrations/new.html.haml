.wrapper
  .header
    .image
      = link_to root_path do
        = image_tag "/logo/logo.png", width:'185px', height:'49px'
    .progress-bar
  .register-main
    .register-container
      .register-head 会員情情報
      .register-form
        .register-form__content
          = form_for(@user, as: resource_name, url: registration_path(resource_name)) do |f|
            = render "devise/shared/error_messages", resource: @user
            .form-group
              = f.label :ニックネーム
              %span.form-require 必須
              %br/
              .input-field
                = f.text_field :nickname, placeholder: "例) フリマ太郎"

            .form-group
              = f.label :メールアドレス
              %span.form-require 必須
              %br/
              .input-field
                = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "PC・携帯どちらでも可"

            .form-group
              = f.label :パスワード
              %span.form-require 必須
              %br/
              .input-field
                = f.password_field :password, autocomplete: "new-password", placeholder: "7文字以上の半角英数字"
            
            .form-group
              = f.label :"パスワード(確認)"
              %span.form-require 必須
              %br/
              .input-field
                = f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "7文字以上の半角英数字"

            .form-group
              %B 本人確認
              %br/
              %p.info-text1 安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。

            .form-group
              = f.label :"お名前(全角)"
              %span.form-require 必須
              %br/
              .input-field
                = f.text_field :first_name, placeholder: "例) 山田"
                = f.text_field :last_name, placeholder: "例) 太郎"
            .form-group
              = f.label :"お名前(カナ)"
              %span.form-require 必須
              %br/
              .input-field
                = f.text_field :first_name_kana, placeholder: "例) ヤマダ"
                = f.text_field :last_name_kana, placeholder: "例) タロウ"
                
            .form-group
              = f.label :"生年月日"
              %span.form-require 必須
              %br/
              .input-field
                = f.date_select(:birthday,with_css_classes:'birthday',use_month_numbers:true, start_year:Time.now.year, end_year:1900, )
            .form-group
              %p.info-text2 ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
            .form-group
              .action
                = f.submit "次へ進む"
  .register-footer